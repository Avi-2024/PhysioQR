import { AdminResourceListPage } from '@/features/admin/components/AdminResourceListPage';

// Renders the backend-backed admin clinic visits list page.
export default function AdminClinicVisitsPage() {
  return <AdminResourceListPage moduleKey="clinicVisits" />;
}
