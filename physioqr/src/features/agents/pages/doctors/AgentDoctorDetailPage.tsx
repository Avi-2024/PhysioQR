import React,{useState}from'react';import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{Link,useNavigate,useParams}from'react-router-dom';import{ArrowLeft,CalendarClock,CheckCircle2,FileUp,MapPin,Stethoscope,UserCheck,Users}from'lucide-react';import apiClient from'@/lib/api-client';import ErrorState from'@/components/feedback/ErrorState';import{Skeleton}from'@/components/ui/Skeleton';
type R=Record<string,unknown>;
export default function AgentDoctorDetailPage(){const{doctorId}=useParams();const navigate=useNavigate();const qc=useQueryClient();const[documentType,setDocumentType]=useState('medical_registration');const[file,setFile]=useState<File|null>(null);const query=useQuery({queryKey:['agent-doctor',doctorId],enabled:Boolean(doctorId),queryFn:async()=>(await apiClient.get(`/agents/me/doctors/${doctorId}`)).data});const upload=useMutation({mutationFn:async()=>{if(!doctorId||!file)throw new Error('Choose a document first');const body=new FormData();body.append('documentType',documentType);body.append('document',file);return apiClient.post(`/agents/me/doctors/${doctorId}/documents`,body,{headers:{'Content-Type':'multipart/form-data'}})},onSuccess:async()=>{setFile(null);await qc.invalidateQueries({queryKey:['agent-doctor',doctorId]})}});if(query.isError)return <ErrorState title="Doctor could not load" message="This doctor may not belong to your agent account, or the record is unavailable." onRetry={()=>query.refetch()}/>;const p=rec(query.data),d=rec(p.doctor),perf=rec(p.performance),visits=arr(p.recentVisits);return <div className="space-y-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><button onClick={()=>navigate('/agent/doctors')} className="inline-flex items-center gap-1 text-sm font-semibold text-neutral-600"><ArrowLeft className="h-4 w-4"/>My Doctors</button>{query.isLoading?<Skeleton className="mt-4 h-9 w-72"/>:<h1 className="mt-3 text-2xl font-bold sm:text-3xl">{text(d.fullName,'Doctor')}</h1>}<p className="mt-1 text-sm text-neutral-500">{text(d.doctorId,'-')}{text(d.clinicName)?` • ${text(d.clinicName)}`:''}</p></div>{!query.isLoading&&<Status value={text(d.status,'draft')}/>}</div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric icon={Stethoscope} label="Approval status" value={label(d.status)} loading={query.isLoading}/><Metric icon={CheckCircle2} label="QR status" value={d.qrCodeActive?'Active':'Not active'} loading={query.isLoading}/><Metric icon={Users} label="Patient registrations" value={num(perf.patientRegistrations)} loading={query.isLoading}/><Metric icon={UserCheck} label="Paid patients" value={num(perf.paidPatients)} loading={query.isLoading}/></div><div className="grid gap-6 xl:grid-cols-[minmax(0,1fr)_380px]"><section className="rounded-xl border border-neutral-200 bg-white p-5 shadow-sm sm:p-6"><h2 className="font-bold">Doctor & Clinic</h2>{query.isLoading?<Skeleton className="mt-4 h-64"/>:<div className="mt-5 grid gap-x-8 gap-y-5 sm:grid-cols-2"><Info label="Mobile" value={d.mobile}/><Info label="WhatsApp" value={d.whatsapp}/><Info label="Email" value={d.email}/><Info label="Qualification" value={d.qualification}/><Info label="Specialization" value={d.specialization}/><Info label="Medical registration" value={d.medicalRegNumber}/><Info label="Registration council" value={d.registrationCouncil}/><Info label="Experience" value={d.yearsOfExperience!==undefined?`${d.yearsOfExperience} years`:undefined}/><Info label="KYC status" value={label(d.kycStatus)}/><Info label="Clinic" value={d.clinicName}/><Info label="Clinic contact" value={d.clinicContact}/><Info label="City / State" value={[text(d.city),text(d.state)].filter(Boolean).join(', ')}/><Info label="Clinic address" value={d.clinicAddress}/><Info label="Working hours" value={d.clinicWorkingHours}/><Info label="Approval date" value={date(d.approvalDate)}/></div>}{text(d.googleMapsLink)&&<a href={text(d.googleMapsLink)} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-primary-700"><MapPin className="h-4 w-4"/>Open clinic location</a>}</section><aside className="space-y-6"><section className="rounded-xl border border-neutral-200 bg-white p-5 shadow-sm"><h2 className="font-bold">Onboarding Documents</h2><p className="mt-1 text-xs text-neutral-500">Upload non-financial doctor documents collected in the field.</p><select value={documentType} onChange={e=>setDocumentType(e.target.value)} className="mt-4 w-full rounded-lg border border-neutral-300 px-3 py-2 text-sm"><option value="medical_registration">Medical registration</option><option value="identity_proof">Identity proof</option><option value="address_proof">Address proof</option><option value="profile_photo">Profile photo</option><option value="other">Other</option></select><input type="file" onChange={e=>setFile(e.target.files?.[0]||null)} className="mt-3 block w-full text-sm"/><button disabled={!file||upload.isPending} onClick={()=>upload.mutate()} className="mt-3 inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-lg bg-primary-600 px-3 text-sm font-semibold text-white disabled:opacity-50"><FileUp className="h-4 w-4"/>{upload.isPending?'Uploading...':'Upload Document'}</button>{upload.error&&<p className="mt-2 text-xs font-semibold text-rose-700">{err(upload.error)}</p>}{upload.isSuccess&&<p className="mt-2 text-xs font-semibold text-emerald-700">Document uploaded successfully.</p>}<p className="mt-3 text-[11px] text-neutral-500">PAN, cancelled cheque and bank documents are intentionally unavailable to agents.</p></section><section className="rounded-xl border border-neutral-200 bg-white p-5 shadow-sm"><div className="flex items-center justify-between"><h2 className="font-bold">Recent Visits</h2><Link to="/agent/clinic-visits" className="text-xs font-semibold text-primary-700">All visits</Link></div><div className="mt-4 space-y-3">{query.isLoading&&<Skeleton className="h-32"/>}{!query.isLoading&&visits.length===0&&<div className="rounded-lg bg-neutral-50 p-4 text-sm text-neutral-500">No clinic visits recorded.</div>}{visits.map(v=><div key={text(v._id||v.id)} className="rounded-lg border border-neutral-200 p-3"><div className="flex gap-2"><CalendarClock className="mt-0.5 h-4 w-4 text-primary-600"/><div><p className="text-sm font-semibold">{date(v.visitDate)} {text(v.visitTime)}</p><p className="text-xs text-neutral-500">{label(v.outcome)}</p>{text(v.nextAction)&&<p className="mt-1 text-xs">Next: {text(v.nextAction)}</p>}</div></div></div>)}</div></section></aside></div><div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-900">This Agent view intentionally excludes doctor banking, fee-share, payout, and confidential patient medical information.</div></div>}
function Metric({icon:Icon,label:lbl,value,loading}:{icon:React.ElementType;label:string;value:string|number;loading:boolean}){return <div className="rounded-xl border border-neutral-200 bg-white p-4 shadow-sm"><Icon className="h-5 w-5 text-primary-600"/>{loading?<Skeleton className="mt-3 h-7 w-24"/>:<p className="mt-3 text-xl font-bold">{value}</p>}<p className="mt-1 text-xs text-neutral-500">{lbl}</p></div>}function Info({label:lbl,value}:{label:string;value:unknown}){return <div><p className="text-xs font-semibold uppercase tracking-wide text-neutral-400">{lbl}</p><p className="mt-1 text-sm font-medium">{text(value,'-')}</p></div>}function Status({value}:{value:string}){const positive=value==='approved',warning=['submitted','under_review','documents_required'].includes(value);return <span className={`rounded-full px-3 py-1.5 text-xs font-bold capitalize ${positive?'bg-emerald-50 text-emerald-700':warning?'bg-amber-50 text-amber-700':'bg-neutral-100 text-neutral-700'}`}>{label(value)}</span>}function rec(v:unknown):R{return v&&typeof v==='object'?v as R:{}}function arr(v:unknown):R[]{return Array.isArray(v)?v.filter(x=>x&&typeof x==='object') as R[]:[]}function text(v:unknown,f=''){return v==null||v===''?f:String(v)}function num(v:unknown){const n=Number(v);return Number.isFinite(n)?n:0}function label(v:unknown){return text(v,'-').replace(/_/g,' ')}function date(v:unknown){if(!v)return'-';const d=new Date(String(v));return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'})}function err(e:unknown){if(e&&typeof e==='object'){const r=(e as {response?:{data?:{message?:unknown}}}).response;return String(r?.data?.message||(e as {message?:unknown}).message||'Upload failed')}return'Upload failed'}
