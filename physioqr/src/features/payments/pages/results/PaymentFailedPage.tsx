import { PaymentFailedPage as PaymentFailedResultPage } from '@/features/payments/pages/PaymentResultPages';

// Renders the payment failed result route from the existing result page.
export default function PaymentFailedPage() {
  return <PaymentFailedResultPage />;
}
