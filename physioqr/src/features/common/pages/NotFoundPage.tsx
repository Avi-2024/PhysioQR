import { NotFoundPage as NotFoundStubPage } from '@/features/common/StubPages';

// Renders the 404 route from the existing common page.
export default function NotFoundPage() {
  return <NotFoundStubPage />;
}
