Objective: Finish PhysioQR Admin frontend UI integration for production-style admin operations.

Audience: Admin users managing healthcare referral, rehabilitation content, doctor approval, finance, safety, support, and audit operations.

Existing app: React/Vite/TypeScript/Tailwind. Work inside D:\jurnary\PhysioQR\physioqr.

Current files:
- src/app/router.tsx
- src/features/admin/pages/AdminApiPages.tsx
- src/features/admin/pages/AdminOperationsPages.tsx
- src/features/admin/pages/AdminDetailPages.tsx
- src/lib/api-client.ts

Direction:
- Keep existing clinical teal SaaS dashboard style.
- Use compact professional admin UX: tables, detail drawer/modals, focused forms, loading/error states.
- Avoid decorative redesign. Wire actual backend endpoints.

Backend contracts to respect:
- POST /doctors/:id/approve
- POST /doctors/:id/reject
- POST /doctors/:id/suspend
- POST /doctors/:id/disable-qr
- POST /doctors/:id/reactivate-qr
- GET /doctors/:id/kyc-documents/:documentId/access
- PATCH /admin/risk-reviews/:id
- PATCH /admin/fraud-cases/:id/review
- GET /admin/audit-logs/export
- POST/PUT /programs and /programs/:id/days
- POST/PUT/DELETE /exercises
- POST/PUT/DELETE /assessments/categories and /assessments/questions
- GET/PUT /settings
- GET /reports/financial, /reports/patients, /reports/programs

Expected output:
- Add missing admin list/detail action flows.
- Add real create/edit forms where feasible for content modules.
- Add secure KYC document access UI.
- Build must pass with npm run build.
